<div class="fixed inset-0 flex items-center justify-center bg-gray-800 bg-opacity-50 z-50">
  <div class="bg-white rounded-2xl shadow-xl p-4 w-3/4 max-h-[80vh] overflow-y-auto">
    <h2 class="text-xl font-semibold mb-4">Pré-visualização da Planilha</h2>

    <%= render "table" %>

    <div class="flex justify-end mt-4">
      <button data-action="click->modal#close" class="bg-blue-500 text-white px-4 py-2 rounded-lg">
        Fechar
      </button>
    </div>
  </div>
</div>
