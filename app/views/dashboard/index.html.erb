<% shortcuts_html = shortcuts %>
<% if Rails.application.secrets.shortcuts_enabled && shortcuts_html.present? %>
  <div class="row">
    <div class="col col-sm-12">
      <legend>Atalhos</legend>
      <div class="row">
        <%= shortcuts_html %>
      </div>
    </div>
  </div>

  <br>
  <br>
<% end %>

<%= render 'teacher_dashboard', teacher: current_teacher %>
