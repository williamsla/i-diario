<% if f.object.persisted? %>
  <fieldset>
    <legend><%= t('.additional_information') %></legend>

    <div class="row">
      <div class="col col-sm-12">
        <%= f.input :opinion, label: aee_label(:opinion, LessonPlan.human_attribute_name(:opinion)), input_html: { class: 'col col-sm-12' }, readonly: current_user.teacher? %>
      </div>
    </div>
  </fieldset>
<% end %>
