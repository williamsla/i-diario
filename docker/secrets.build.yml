# Apenas para build (docker build / rake assets:precompile). Não usar em runtime.
production:
  secret_key_base: build_placeholder_replace_at_deploy_with_real_secret_key_base
  cache_store_url: memcached://127.0.0.1:11211
  REDIS_MODE: standalone
  REDIS_URL: redis://127.0.0.1:6379
  REDIS_DB_CACHE: "/0"
  REDIS_DB_SESSION: "/1"
  REDIS_DB_SIDEKIQ: "/2"
  REDIS_SENTINELS: ""
  SMTP_ADDRESS: localhost
  SMTP_PORT: "25"
  SMTP_DOMAIN: localhost
  SMTP_USER_NAME: ""
  SMTP_PASSWORD: ""
  NO_REPLY_ADDRESS: noreply@example.invalid
  STUDENT_DOMAIN: localhost
  EMAIL_SKIP_DOMAINS: ""
  shortcuts_enabled: false
  only_one_conceptual_avaliation: false
  conceptual_exam_batch_layout: false
  show_objectives: true
  sidekiq_password: build_only
